import 'package:flutter/cupertino.dart';
import 'package:get/get.dart';

class Routes {
  static const LOGIN = '/login';
  static const HOME = '/home';
  static const FORGOT_PASSWORD = '/forgotpassword';

  // static Route<dynamic>? routesNavigaion(RouteSettings settings) {
  //   if (settings.name == Routes.FORGOT_PASSWORD) {
  //     return GetPageRoute(
  //         settings: settings, page: () => ForgotPasswordScreen());
  //   }
  //
  //   if (settings.name == Routes.LOGIN) {
  //     return GetPageRoute(settings: settings, page: () => LoginScreen());
  //   }
  //   if (settings.name == Routes.HOME) {
  //     return GetPageRoute(
  //         settings: settings, page: () => MainScreen(currentIndex: 0));
  //   }
  //   return null;
  // }
}
